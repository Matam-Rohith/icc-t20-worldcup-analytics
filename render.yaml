services:
  - type: web
    name: icc-t20-worldcup-analytics
    env: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
